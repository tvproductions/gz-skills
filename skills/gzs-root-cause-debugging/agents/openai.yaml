interface:
  display_name: "GovZero Root-Cause Debugging"
  short_description: "Find root causes before changing code"
  default_prompt: "Use $gzs-root-cause-debugging to diagnose this failure from reproducible evidence before proposing a fix."
policy:
  allow_implicit_invocation: true
